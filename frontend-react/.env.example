# Copy to .env.local for local dev — Vite reads this automatically.
# In production, set VITE_API_BASE in the Vercel dashboard under
# Project Settings → Environment Variables.
VITE_API_BASE=http://localhost:8000
