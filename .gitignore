# Secrets — never commit
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/
env/

# Data — training artifacts, too large for git
data/raw/news/
data/raw/prices/
data/interim/
# ...except the sentiment cache, which the Docker image ships so the API
# never has to score a cold cache with FinBERT on a 1GB container.
!data/interim/
!data/interim/sentiment_cache.parquet
data/processed/

# Keep the demo-day live snapshots (they're tiny and the fallback needs them)
!data/raw/live/

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Node (frontend)
frontend-react/node_modules/
frontend/  

# IDE
.vscode/
.idea/
